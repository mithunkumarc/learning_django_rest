#### validation fields (incoming data) : done at serializers.py file

priority : need to verify : may be depends of order(which one appear first)

1. validators
2. field level validation
3. object level validation

  
